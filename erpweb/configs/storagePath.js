const companyProfile = 'company';

const profile = 'manager/profileImage';
const certCrime = 'manager/certCrime';
const certHealth = 'manager/certHealth';
const certName = 'manager/certName';

const review = 'review';

const storagePath = {
  companyProfile,
  manager: {
    profile,
    certCrime,
    certHealth,
    certName,
  },
  review,
};

export default storagePath;
