const eventDefault = 'event';
const eventBanner = 'event/banner';
const eventDetail = 'event/detail';
const eventThumbnail = 'event/thumbnail';

const tipDefault = 'tip';
const tipImages = 'tip/images';
const tipProfile = 'tip/profile';

const noticeThumbnail = 'notice/thumbnail';

const storagePathConfigs = {
  eventDefault,
  eventBanner,
  eventDetail,
  eventThumbnail,

  tipDefault,
  tipImages,
  tipProfile,

  noticeThumbnail,
};

export default storagePathConfigs;
